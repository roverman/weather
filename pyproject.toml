[project]
name = "weather"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Wei Tang", email = "68605140+roverman@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.2.0",
]

[project.scripts]
weather = "weather:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
